// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"orkester/ent/searchpath"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// SearchPathCreate is the builder for creating a SearchPath entity.
type SearchPathCreate struct {
	config
	mutation *SearchPathMutation
	hooks    []Hook
}

// SetPath sets the "path" field.
func (spc *SearchPathCreate) SetPath(s string) *SearchPathCreate {
	spc.mutation.SetPath(s)
	return spc
}

// Mutation returns the SearchPathMutation object of the builder.
func (spc *SearchPathCreate) Mutation() *SearchPathMutation {
	return spc.mutation
}

// Save creates the SearchPath in the database.
func (spc *SearchPathCreate) Save(ctx context.Context) (*SearchPath, error) {
	return withHooks(ctx, spc.sqlSave, spc.mutation, spc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (spc *SearchPathCreate) SaveX(ctx context.Context) *SearchPath {
	v, err := spc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (spc *SearchPathCreate) Exec(ctx context.Context) error {
	_, err := spc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (spc *SearchPathCreate) ExecX(ctx context.Context) {
	if err := spc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (spc *SearchPathCreate) check() error {
	if _, ok := spc.mutation.Path(); !ok {
		return &ValidationError{Name: "path", err: errors.New(`ent: missing required field "SearchPath.path"`)}
	}
	return nil
}

func (spc *SearchPathCreate) sqlSave(ctx context.Context) (*SearchPath, error) {
	if err := spc.check(); err != nil {
		return nil, err
	}
	_node, _spec := spc.createSpec()
	if err := sqlgraph.CreateNode(ctx, spc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	spc.mutation.id = &_node.ID
	spc.mutation.done = true
	return _node, nil
}

func (spc *SearchPathCreate) createSpec() (*SearchPath, *sqlgraph.CreateSpec) {
	var (
		_node = &SearchPath{config: spc.config}
		_spec = sqlgraph.NewCreateSpec(searchpath.Table, sqlgraph.NewFieldSpec(searchpath.FieldID, field.TypeInt))
	)
	if value, ok := spc.mutation.Path(); ok {
		_spec.SetField(searchpath.FieldPath, field.TypeString, value)
		_node.Path = value
	}
	return _node, _spec
}

// SearchPathCreateBulk is the builder for creating many SearchPath entities in bulk.
type SearchPathCreateBulk struct {
	config
	err      error
	builders []*SearchPathCreate
}

// Save creates the SearchPath entities in the database.
func (spcb *SearchPathCreateBulk) Save(ctx context.Context) ([]*SearchPath, error) {
	if spcb.err != nil {
		return nil, spcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(spcb.builders))
	nodes := make([]*SearchPath, len(spcb.builders))
	mutators := make([]Mutator, len(spcb.builders))
	for i := range spcb.builders {
		func(i int, root context.Context) {
			builder := spcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*SearchPathMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, spcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, spcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, spcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (spcb *SearchPathCreateBulk) SaveX(ctx context.Context) []*SearchPath {
	v, err := spcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (spcb *SearchPathCreateBulk) Exec(ctx context.Context) error {
	_, err := spcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (spcb *SearchPathCreateBulk) ExecX(ctx context.Context) {
	if err := spcb.Exec(ctx); err != nil {
		panic(err)
	}
}
